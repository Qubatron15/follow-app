---
export const prerender = false;

// Redirect to /threads if authenticated, otherwise to /login
const user = Astro.locals.user;

if (user) {
  return Astro.redirect("/threads");
} else {
  return Astro.redirect("/login");
}
---
